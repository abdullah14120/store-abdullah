pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // مستودع JitPack ضروري جداً لتحميل مكتبة Fetch2 ومكتبات الصور
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "FIX ENGINE"
include ':app'
