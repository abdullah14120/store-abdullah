pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS) // غيرنا هذه لتكون الأولوية للإعدادات هنا
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' } // مستودع المكتبات الخارجية
    }
}
rootProject.name = "FIX ENGINE"
include ':app'
