pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        
        // هذا السطر هو "المفتاح" لحل مشكلتك الآن
        maven { url 'https://jitpack.io' }
    }
}

rootProject.name = "FIX ENGINE"
include ':app'
