<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- الوصول للإنترنت وحالة الشبكة -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- صلاحيات التخزين للأنظمة القديمة والحديثة -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    
    <!-- الصلاحية الأهم: السماح للمتجر بتثبيت حزم APK -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- صلاحية الإشعارات لأندرويد 13 فما فوق -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- للسماح بالتحميل المستقر في الخلفية -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="متجر عبدالله التميمي"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FixEngine"
        android:usesCleartextTraffic="true"
        tools:targetApi="35">

        <!-- الواجهة الرئيسية للمتجر -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustPan">
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- دعم الروابط العميقة: fixengine://details?pkg=com.example.app -->
            <intent-filter android:label="فتح في متجر عبدالله التميمي">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                
                <data 
                    android:scheme="fixengine" 
                    android:host="details" />
            </intent-filter>
        </activity>

        <!-- واجهة تفاصيل التطبيق -->
        <activity
            android:name=".ui.details.AppDetailsActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- مستقبل إشعارات اكتمال التحميل -->
        <receiver
            android:name=".service.DownloadReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.DOWNLOAD_COMPLETE" />
            </intent-filter>
        </receiver>

        <!-- موفر الملفات: ضروري لمشاركة الـ APK مع مثبت النظام -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- تعريف خدمة WorkManager لضمان عمل التحديثات في الخلفية -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup" />
        </provider>

    </application>

</manifest>
