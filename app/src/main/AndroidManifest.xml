<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.fix.engine.abdullah">

    <!-- ========================================== -->
    <!-- 🛡️ Core Permissions (System & Networking)  -->
    <!-- ========================================== -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- Storage Configurations (Scoped Storage Compliance) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    
    <!-- Package Management & Installation -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" tools:ignore="QueryAllPackagesPermission" />

    <!-- Android 13+ (API 33) Requirements -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Android 14+ (API 34) Foreground Service Requirements -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>
    </queries>

    <!-- ========================================== -->
    <!-- 🏗️ Application Level Configuration          -->
    <!-- ========================================== -->
    <application
        android:name=".AbdullahStoreApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="متجر Abdullah"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FixEngine"
        android:networkSecurityConfig="@xml/network_security_config"
        android:hardwareAccelerated="true"
        tools:targetApi="35">
 
        <!-- ========================================== -->
        <!-- 🎨 UI Architecture (Activities)            -->
        <!-- ========================================== -->
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustPan">
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep Linking Configuration -->
            <intent-filter android:label="فتح في متجر Abdullah">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="fixengine" android:host="details" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.details.AppDetailsActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.FixEngine" />


        <!-- ========================================== -->
        <!-- ⚙️ Core Logic (Providers, Receivers, Svcs) -->
        <!-- ========================================== -->

        <!-- Secure File Provider for Internal/External Sharing -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- AndroidX Startup Provider for WorkManager -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup" />
        </provider>
        
        <!-- Package Installer Status Receiver (Explicit Scope) -->
        <receiver 
            android:name=".installer.InstallStatusReceiver" 
            android:exported="false">
            <intent-filter>
                <action android:name="com.fix.engine.abdullah.COMMIT_INSTALL" />
            </intent-filter>
        </receiver>

        <!-- Fetch2 Download Service (API 34 Data Sync Type Bound) -->
        <service
            android:name="com.tonyodev.fetch2.FetchService"
            android:foregroundServiceType="dataSync"
            android:exported="false"
            tools:node="merge" />

    </application>
</manifest>
